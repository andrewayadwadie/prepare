import 'package:flutter/material.dart';

class MapBoxDirection extends StatelessWidget {
  const MapBoxDirection({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child:Container()),
    );
  }
}